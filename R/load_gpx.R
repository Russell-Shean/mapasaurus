#' Load .gpx files from disk
#'
#' @description
#' This function loads a locally stored .gpx file and returns a simple features (sf) dataframe. 
#'
#' @param path A character vector with one element. This should be the path to a locally stored gpx file.
#'
#' @return A simple features (sf) dataframe
#' @export
#' @examples   
#' activity1 <- load_gpx("C:/Users/my_user_name/activities/activity1.gpx")

load_gpx <- function(path){
  
  
  print(path)
  
}